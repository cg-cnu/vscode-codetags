{
    "name": "vscode-notes",
    "displayName": "vscode-notes",
    "description": "insert tags in the code",
    "version": "0.0.1",
    "publisher": "sreenivas alapati",
    "engines": {
        "vscode": "^1.16.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:codeTags.todo",
        "onCommand:codeTags.issue"
    ],
    "main": "./out/src/codeTags",
    "contributes": {
        "commands": [
            {
                "command": "codeTags.todo",
                "title": "Note: Todo"
            },
            {
                "command": "codeTags.issue",
                "title": "Note: Issue"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.5.2",
        "vscode": "^1.1.5",
        "mocha": "^3.5.0",
        "@types/node": "^7.0.43",
        "@types/mocha": "^2.2.42"
    }
}
